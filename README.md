# HousePrice_LinearRegression
Implemented a Multiple Linear Regression model using the Housing dataset. Performed data preprocessing, encoded categorical features, split data into training and testing sets, trained the model using Scikit-learn, evaluated performance with MAE, MSE, and R² score, and visualized results using Actual vs Predicted and Residual plots.
